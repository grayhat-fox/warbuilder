import 'package:fluent_ui/fluent_ui.dart';

class FeaturesModuleWidget extends StatelessWidget {
  const FeaturesModuleWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
